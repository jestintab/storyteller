#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/bash" ]; then
  "$basedir//bin/bash"  "$basedir/../nodeman/bin/nodeman" "$@"
  ret=$?
else 
  /bin/bash  "$basedir/../nodeman/bin/nodeman" "$@"
  ret=$?
fi
exit $ret
